<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.compscieddy.timetracker"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".MyApplication"
        android:windowSoftInputMode="adjustResize">

        <meta-data android:name="DATABASE" android:value="time_tracker.db" />
        <meta-data android:name="VERSION" android:value="9" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.compscieddy.timetracker" />

        <activity android:name=".DotsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
